import React from "react";
// import faceBookLogo from "../../assets/f_logo_RGB-Blue_512.png";
import { FiFacebook } from "react-icons/fi";

export const FacebookLogo = ({ fontSize }) => {
  // return <img width={width} alt="Facebook Logo" src={faceBookLogo} />;
  return (
    <FiFacebook
      fontSize={fontSize}
      color="#4267B2
  "
    />
  );
};
