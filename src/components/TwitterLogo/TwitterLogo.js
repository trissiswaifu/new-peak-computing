import React from "react";
import twitterLogo from "../../assets/twitterlogo.png";

export const TwitterLogo = ({ width }) => {
  return <img width={width} alt="Twiiter Logo" src={twitterLogo} />;
};
